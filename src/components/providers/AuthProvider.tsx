// TODO: Dev 1 — AuthProvider
// Wraps app with Firebase Auth context
'use client'

import { type ReactNode } from 'react'

export function AuthProvider({ children }: { children: ReactNode }) {
  // TODO: implement Firebase Auth listener
  return <>{children}</>
}
