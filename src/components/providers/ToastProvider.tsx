// TODO: Dev 1 — ToastProvider
// Global toast notification container
'use client'

import { type ReactNode } from 'react'

export function ToastProvider({ children }: { children: ReactNode }) {
  // TODO: render toast container + children
  return <>{children}</>
}
