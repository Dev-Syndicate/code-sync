// TODO: Dev 3 — Session/Editor page
export default function SessionPage({
  params,
}: {
  params: Promise<{ sessionId: string }>
}) {
  return (
    <div>
      <h1>Session</h1>
      {/* TODO: Add editor, chat, participant list */}
    </div>
  )
}
