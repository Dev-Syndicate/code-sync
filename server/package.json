{
  "name": "code-sync-yjs-server",
  "version": "0.1.0",
  "private": true,
  "description": "Yjs WebSocket collaboration server for CodeSync — relays CRDT updates between connected clients and keeps an authoritative in-memory doc per session.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch yjs-server.ts",
    "start": "node --import tsx yjs-server.ts"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "y-protocols": "^1.0.6",
    "y-websocket": "^2.0.4",
    "yjs": "^13.6.30"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
